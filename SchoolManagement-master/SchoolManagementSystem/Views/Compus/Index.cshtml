@model IEnumerable<SchoolManagementSystem.Domain.Entities.Compus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.compusnam)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.compusnam)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.compus_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.compus_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.compus_id })
        </td>
    </tr>
}

</table>
